[package]
name = "gdcleaner"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = "1.50.0"
clap = {version = "4.6.0", features = ["derive"]}
toml = "1.1.0"
serde = { version = "1.0.228", features = ["derive"]}
